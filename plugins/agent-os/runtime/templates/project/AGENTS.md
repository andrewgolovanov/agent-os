# Project instructions

## Scope

These instructions apply to this project wrapper. Replace template values during onboarding; do not leave invented facts.

## Before substantive work

- Resolve this project in `../../config/projects.yaml` and confirm the wrapper path.
- Read `project.yaml` and `docs/PROJECT.md` when it contains verified context.
- Enter the exact target repository, verify its root, remotes, branch, HEAD, and worktree status, then read its closest `AGENTS.md`.
- Treat repository code and external provider state as evidence, not as permission to mutate unrelated systems.

## Project rules

- Source of truth: `unknown`.
- Default publication target: `unknown`.
- External sources: `unknown`.
- Project-specific tools: none until a repeatable workflow is verified.

## Validation

Use the target repository's own documented checks. Report verified paths and commands; never infer equivalence between repositories from similar names.
