schema_version: 1

agent_os:
  name: agent-os
  root: __AGENT_OS_HOME__
  timezone: __TIMEZONE__

# Projects may have repositories: []. Add them through automatic Codex sync or
# the preview-first onboard-project workflow after verifying exact local roots.
projects: {}
