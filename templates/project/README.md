# Project Name

Коротко опишите, что объединяет этот project wrapper и зачем он существует.

## Start here

1. Read `AGENTS.md`.
2. Confirm this project in `../../config/projects.yaml`.
3. Open the exact repository path from `project.yaml`.
4. Read the repository's closest `AGENTS.md` before edits.

## Repositories

| ID | Role | Source of truth | Primary branch |
| --- | --- | --- | --- |
| `unknown` | `unknown` | `unknown` | `unknown` |

## Documentation

- `docs/PROJECT.md` — durable product and topology context.
- `project.yaml` — structured local project metadata.
- `tools/` — verified project-only helpers.
