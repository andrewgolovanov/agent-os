interface:
  display_name: "Onboard Project"
  short_description: "Register a repository from any local folder"
  default_prompt: "Use $onboard-project to register this repository directly in Agent OS without moving it."
